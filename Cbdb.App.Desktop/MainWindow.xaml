<Window x:Class="Cbdb.App.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="NAVIGATION_PANE"
        Height="780"
        Width="1000"
        MinHeight="680"
        MinWidth="920"
        Background="#F2F2F2">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="150" />
        </Grid.ColumnDefinitions>

        <Grid Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    HorizontalAlignment="Center"
                    Background="#133FB5"
                    Padding="16,4">
                <TextBlock Name="TxtHeaderMain"
                           FontSize="34"
                           FontWeight="Bold"
                           Foreground="White"
                           Text="中國歷代人物傳記資料庫" />
            </Border>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Button Name="BtnLangEn" Width="40" Height="28" Margin="4,0,0,0" Click="BtnLangEn_Click" Content="EN" />
                <Button Name="BtnLangZhHant" Width="40" Height="28" Margin="4,0,0,0" Click="BtnLangZhHant_Click" Content="繁" />
                <Button Name="BtnLangZhHans" Width="40" Height="28" Margin="4,0,0,0" Click="BtnLangZhHans_Click" Content="简" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1"
                Grid.ColumnSpan="2"
                Margin="0,12,0,10"
                Padding="10"
                BorderBrush="#D0D0D0"
                BorderThickness="1"
                Background="White">
            <DockPanel>
                <Button Name="BtnRelinkTables"
                        Height="34"
                        MinWidth="260"
                        HorizontalAlignment="Left"
                        Click="BtnRelinkTables_Click" />
            </DockPanel>
        </Border>

        <Border Grid.Row="2"
                Grid.Column="0"
                Margin="4,0,14,0"
                Padding="20,16"
                BorderBrush="Black"
                BorderThickness="1"
                Background="#F9F9F9">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Button Name="BtnModuleBrowser" Height="40" Margin="0,4" Click="ModuleButton_Click" Tag="module.browser" />
                    <Button Name="BtnModuleEntry" Height="40" Margin="0,4" Click="ModuleButton_Click" Tag="module.entry" />
                    <Button Name="BtnModuleOffice" Height="40" Margin="0,4" Click="ModuleButton_Click" Tag="module.office" />
                    <Button Name="BtnModuleKinship" Height="40" Margin="0,4" Click="ModuleButton_Click" Tag="module.kinship" />
                    <Button Name="BtnModuleAssociations" Height="40" Margin="0,4" Click="ModuleButton_Click" Tag="module.associations" />
                    <Button Name="BtnModuleNetworks" Height="40" Margin="0,4" Click="ModuleButton_Click" Tag="module.networks" />
                    <Button Name="BtnModuleAssociationPairs" Height="40" Margin="0,4" Click="ModuleButton_Click" Tag="module.association_pairs" />
                    <Button Name="BtnModulePlace" Height="40" Margin="0,4" Click="ModuleButton_Click" Tag="module.place" />
                    <Button Name="BtnModuleStatus" Height="40" Margin="0,4" Click="ModuleButton_Click" Tag="module.status" />
                    <Button Name="BtnModuleTexts" Height="40" Margin="0,4" Click="ModuleButton_Click" Tag="module.texts" />
                    <Button Name="BtnReportError" Height="40" Margin="0,16,0,4" Click="BtnReportError_Click" />
                    <Button Name="BtnChangeIndexAddress" Height="40" Margin="0,4" Click="BtnChangeIndexAddress_Click" />
                </StackPanel>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2"
                    Grid.Column="1"
                    VerticalAlignment="Bottom"
                    Margin="0,0,0,10">
            <Button Name="BtnUsersGuide" Height="46" Margin="0,0,0,10" Click="BtnUsersGuide_Click" />
            <Button Name="BtnExit" Height="36" Click="BtnExit_Click" />
        </StackPanel>

        <Border Grid.Row="3"
                Grid.ColumnSpan="2"
                Margin="0,12,0,0"
                Padding="8"
                BorderBrush="#D0D0D0"
                BorderThickness="1"
                Background="White">
            <StackPanel>
                <TextBlock Name="TxtStatus" FontWeight="SemiBold" Text="Ready" />
                <TextBox Name="TxtOutput"
                         Margin="0,6,0,0"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         FontSize="12"
                         MinHeight="86"
                         MaxHeight="120" />
            </StackPanel>
        </Border>
    </Grid>
</Window>


